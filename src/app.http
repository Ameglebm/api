### 🎬 CINEMA TICKET API - Endpoints
@baseUrl = http://localhost:3000

# ============================================
# 🏥 HEALTH CHECK
# ============================================

### Health Check da API
GET {{baseUrl}}/health
Content-Type: application/json

# ============================================
# 📋 SESSIONS
# ============================================

### 🔹 Criar nova sessão
# @name createSession
POST {{baseUrl}}/sessions
Content-Type: application/json

{
  "movie": "Avatar: O Caminho da Água",
  "room": "Sala 3",
  "startsAt": "2026-02-20T19:00:00.000Z",
  "ticketPrice": 25.00,
  "totalSeats": 20
}

### 🔹 Listar todas as sessões
GET {{baseUrl}}/sessions
Content-Type: application/json

### 🔹 Buscar sessão por ID
# Cole o ID retornado no createSession
@sessionId = {{createSession.response.body.id}}
GET {{baseUrl}}/sessions/{{sessionId}}
Content-Type: application/json

### 🔹 Buscar sessão inexistente (deve retornar 404)
GET {{baseUrl}}/sessions/id-que-nao-existe
Content-Type: application/json

### 🔹 Criar sessão com totalSeats abaixo do mínimo (deve retornar 400)
POST {{baseUrl}}/sessions
Content-Type: application/json

{
  "movie": "Teste Validação",
  "room": "Sala 1",
  "startsAt": "2026-02-20T19:00:00.000Z",
  "ticketPrice": 25.00,
  "totalSeats": 5
}

### 🔹 Criar sessão com campo obrigatório faltando (deve retornar 400)
POST {{baseUrl}}/sessions
Content-Type: application/json

{
  "room": "Sala 1",
  "startsAt": "2026-02-20T19:00:00.000Z",
  "ticketPrice": 25.00,
  "totalSeats": 20
}

### 🔹 Criar sessão com ticketPrice negativo (deve retornar 400)
POST {{baseUrl}}/sessions
Content-Type: application/json

{
  "movie": "Teste Preço",
  "room": "Sala 1",
  "startsAt": "2026-02-20T19:00:00.000Z",
  "ticketPrice": -10,
  "totalSeats": 20
}